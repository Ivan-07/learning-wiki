# 学习机制三项高价值升级：系统设计思路

> 文档性质：现有 Learning Wiki 简化方案的学习系统增量设计
> 设计范围：能力目标、误解模型、迁移与真实应用
> 适用场景：单用户、本地优先、Obsidian + Git
> 核心约束：复用现有知识库的来源、Evidence、Wiki 和版本体系，不建立第二套知识事实源

---

## 0. 设计结论

当前学习机制已经具备闭卷回答、反馈、复习和应用的基础闭环，但仍然偏向：

```text
围绕资料或概念
→ 生成学习对象
→ 回答
→ 根据结果安排复习
```

三项升级之后，学习系统应转变为：

```text
能力目标决定学到什么程度
→ 起点诊断定位最小知识缺口
→ 误解模型决定下一步练什么
→ 迁移阶梯验证能否在新情境中使用
→ 真实应用验证知识是否改善行动
```

系统最终维护两类彼此分离的数据：

1. **知识状态**：来源、证据、Wiki、争议、版本和时效；
2. **学习者状态**：目标、尝试、误解、迁移证据和应用结果。

学习者答错通常意味着学习者需要纠正，不意味着知识库错误。学习结果只能生成知识更新候选，不能直接修改 Wiki。

---

## 1. 升级目标与设计原则

### 1.1 三项升级分别解决什么问题

| 升级 | 当前风险 | 目标 |
|---|---|---|
| 能力目标 | 学完材料却不知道形成了什么能力 | 以可观察表现定义完成 |
| 误解模型 | 只记录错题，下一次仍重复同类练习 | 根据错误结构选择纠正策略 |
| 迁移与应用 | 能回忆和解释，但遇到真实问题不会用 | 用陌生案例和真实项目验证能力 |

### 1.2 共同原则

1. 不以阅读完成、页面打开或 AI 摘要数量作为学习证据。
2. 不以单一百分比宣称掌握。
3. 每个高水平状态必须能回到具体的用户行为记录。
4. 所有反馈和标准都要绑定具体 Evidence 或明确标记为推断。
5. 历史回答不可修改；纠正、反馈和复评使用追加事件。
6. 学习对象绑定知识版本。知识更新后不改写历史学习记录。
7. 一次学习会话只推进一个主要能力目标。
8. 学习系统优先减少无效练习，而不是增加题目数量。
9. 真实应用由用户确认，系统不能仅根据文件引用自动认定成功。
10. 三项升级都必须有渐进启用路径，避免首版复杂度重新暴涨。

### 1.3 非目标

本设计暂不实现：

- 完整课程管理系统；
- 通用教育测量平台；
- 自动生成庞大的先修知识图谱；
- 贝叶斯知识追踪或深度学习推荐；
- 排行榜、积分、连续打卡；
- 由 AI 自动认证专业能力；
- 无人工确认的工作成果评价；
- 多用户教学和教师管理。

---

## 2. 总体结构

### 2.1 学习系统与知识底座

```text
┌─────────────────────────────────────────────┐
│ Knowledge Core                              │
│ SourceVersion · EvidenceRef · Wiki · Update │
└─────────────────────┬───────────────────────┘
                      │ versioned evidence
                      ▼
┌─────────────────────────────────────────────┐
│ Learning Orchestrator                       │
│ 目标解析 · 诊断 · 活动选择 · 状态推进        │
├──────────────┬──────────────┬───────────────┤
│ Capability   │ Misconception│ Transfer      │
│ Goals        │ Model        │ & Application │
└──────────────┴──────────────┴───────────────┘
                      │ append-only events
                      ▼
┌─────────────────────────────────────────────┐
│ Learning Store                              │
│ Goals · Attempts · Misconceptions · Evidence│
└─────────────────────────────────────────────┘
```

### 2.2 三项升级之间的关系

能力目标回答：

> 用户最终应该能够做什么。

误解模型回答：

> 用户为什么还做不到，以及下一步最应该纠正什么。

迁移和应用回答：

> 用户能否在脱离原题的新情境中真正做到。

三者组成决策闭环：

```text
能力目标
→ 起点诊断
→ 暴露知识缺口或误解
→ 选择针对性练习
→ 近迁移
→ 远迁移
→ 真实应用
→ 更新能力证据
→ 决定结束、继续或调整目标
```

---


## 3. 升级一：能力目标

### 3.1 为什么需要能力目标

“学习一篇文章”“学习检索练习”都不是可验证的完成条件。

能力目标必须描述用户在某种约束下能够完成的行为。例如：

- 能用自己的话解释检索练习为何有效；
- 能区分重新阅读、检索练习和交错练习；
- 能为一项真实学习任务设计复习安排；
- 能判断什么场景不适合使用某个决策模型；
- 能在没有打开知识库时完成一次方法选择。

### 3.2 目标结构

`40 Learning/Goals/{goal_id}.yaml`：

```yaml
schema_version: 1
goal_id: goal_learning_plan_001
title: 为不同知识设计合适的复习计划
status: active
created_at: 2026-09-04T10:00:00+08:00
target_date: 2026-10-01
importance: high

context:
  project_ids:
    - project_personal_learning
  motivation: 为自己的长期学习建立可持续安排

capabilities:
  - capability_id: cap_explain_retrieval
    behavior: 能解释检索练习为何改善长期保持
    required_level: explain
  - capability_id: cap_discriminate_methods
    behavior: 能根据案例区分重新阅读、检索和交错练习
    required_level: discriminate
  - capability_id: cap_design_plan
    behavior: 能为真实材料设计复习计划
    required_level: real_application

knowledge_dependencies:
  note_ids:
    - concept_retrieval_practice
    - concept_spacing_effect
  evidence_ids:
    - ev_src01_v0001_0042

completion_policy:
  require_all_capabilities: true
  max_evidence_age_days: 90
  require_closed_book: true
  require_user_confirmed_application: true
```

### 3.3 能力层级

系统采用可解释的离散层级，而不是总分：

| 层级 | 含义 | 最低证据 |
|---|---|---|
| `unseen` | 尚无学习记录 | 无 |
| `recognize` | 能识别术语或正确选项 | 识别任务 |
| `recall` | 能闭卷回忆关键内容 | 无提示自由回答 |
| `explain` | 能解释机制、原因和边界 | rubric 覆盖解释 |
| `discriminate` | 能与相似概念正确区分 | 对比或混合案例 |
| `near_transfer` | 能处理与学习案例相似的问题 | 新表述、相似结构 |
| `far_transfer` | 能处理表面不同但结构相同的问题 | 陌生领域案例 |
| `real_application` | 在真实项目中正确使用 | 用户确认的应用事件 |

能力状态只表示“当前存在的最高证据”，不表示永久掌握。

### 3.4 起点诊断

新目标开始时执行最小诊断：

1. 一个开放式解释或自由回忆；
2. 一个概念辨别；
3. 一个简短应用判断；
4. 每题填写 0–100 的置信度；
5. 记录是否打开过来源或使用提示。

诊断输出：

```yaml
goal_id: goal_learning_plan_001
diagnostic_summary:
  strongest_capability: recall
  weakest_capability: discriminate
  suspected_prerequisite_gaps:
    - concept_desirable_difficulty
  suspected_misconceptions:
    - fluent_means_learned
  recommended_next_activity: contrast_case
```

诊断不生成“通过/失败”的总结果，只用于选择下一步。

### 3.5 最小知识缺口路由

| 当前表现 | 下一步 |
|---|---|
| 无法回忆核心概念 | 提供最小必要材料，然后再次回忆 |
| 能回忆但不能解释 | 自我解释、因果链、为什么问题 |
| 能解释但边界模糊 | 反例、适用条件、失败案例 |
| 容易混淆相似概念 | 对比表、交错案例、强制选择理由 |
| 理解但不会使用 | 近迁移案例 |
| 近迁移成功 | 远迁移或真实项目 |
| 真实应用成功 | 延迟再验证或结束目标 |

系统应跳过已有充分证据的低层练习。

### 3.6 目标状态机

```text
draft
→ active
→ diagnosing
→ learning
→ transfer_testing
→ application_pending
→ achieved
```

旁路状态：

```text
paused
abandoned
needs_revalidation
```

进入 `achieved` 的必要条件：

- 每个必需能力达到目标层级；
- 证据没有超过允许年龄；
- 高层能力没有完全依赖提示；
- 要求真实应用的目标已有用户确认事件；
- 相关知识版本没有被标记为过期或争议升级。

### 3.7 目标拆分规则

一个目标如果同时包含超过五个独立能力，系统建议拆分。

拆分原则：

- 每个目标可以在一至四周内验证；
- 每项能力可以被一个明确活动检验；
- 能力之间存在先后关系时，先形成轻量依赖；
- 不为每个知识点创建目标；
- 目标应来自项目、决策、写作、长期兴趣或重复误解。

---

## 4. 升级二：误解模型

### 4.1 从错题到错误结构

传统错题簿记录“某次回答错了”。误解模型记录：

- 用户原本相信什么；
- 在什么条件下会触发错误；
- 错误属于哪种认知结构；
- 用户对错误有多自信；
- 哪种干预已经尝试；
- 错误是否在新情境中再次出现；
- 什么证据可以认为误解已经改善。

### 4.2 第一版错误分类

| 类型 | 描述 | 典型纠正活动 |
|---|---|---|
| `fact_gap` | 缺少必要事实 | 短回忆、最小材料 |
| `causal_gap` | 记得结论但无法解释机制 | 因果链、自我解释 |
| `boundary_error` | 忽略适用条件和边界 | 反例、条件变化 |
| `concept_confusion` | 混淆相似概念 | 对比、交错练习 |
| `proxy_confusion` | 把替代指标误当目标 | 指标反例、结果对照 |
| `overgeneralization` | 将局部结论推广过度 | 范围缩放、例外案例 |
| `unsupported_inference` | 推断没有证据支持 | 证据分类、来源检查 |
| `procedure_error` | 知道步骤但执行顺序或条件错误 | 实际操作、步骤诊断 |
| `application_mismatch` | 选择了不适合当前情境的方法 | 场景辨别、决策树 |
| `outdated_knowledge` | 使用了已经更新的旧结论 | 新旧版本差异练习 |

第一版只允许使用这组封闭类型；新增类型必须有重复真实样本支持。

### 4.3 误解对象

`40 Learning/Misconceptions/{misconception_id}.yaml`：

```yaml
schema_version: 1
misconception_id: mis_fluent_means_learned
status: open
created_at: 2026-09-04T11:00:00+08:00

concept_ids:
  - concept_retrieval_practice
  - concept_fluency_illusion

statement:
  user_belief: 学习时越流畅，长期记忆效果通常越好
  correction: 学习时的主观流畅性不能单独预测延迟保持
  error_type: proxy_confusion

evidence_ids:
  - ev_src01_v0001_0042

observations:
  first_event_id: evt_01J...
  last_event_id: evt_01K...
  occurrences: 2
  max_confidence_when_wrong: 90
  contexts:
    - 选择复习方法
    - 判断学习效果

intervention_history:
  - activity: direct_explanation
    result: recurred
  - activity: contrast_case
    result: improving

resolution_policy:
  require_two_non_identical_contexts: true
  require_closed_book: true
```

### 4.4 误解创建规则

以下情况创建候选而不是立即确认误解：

- 一次事实错误；
- 一次低置信度猜测；
- 一次表达不清但方向正确；
- 评分模型自身不确定。

以下情况可以确认误解：

- 同一错误结构出现两次；
- 一次高置信度错误；
- 用户明确确认自己原先持有该错误信念；
- 在真实应用中因该错误导致错误决策；
- 不同题面中重复出现相同错误。

AI 提议错误类型，确定性服务保存结构；用户可以修改或拒绝。

### 4.5 干预策略

误解纠正遵循：

```text
再次暴露原认知
→ 让用户明确表达预测
→ 提供能够区分新旧解释的证据
→ 要求用户解释冲突
→ 在新题面中重新判断
→ 延迟后再验证
```

不要只展示“正确答案”。单纯重复解释如果已经失败，下一次必须改变活动类型。

### 4.6 高置信度错误

当 `confidence >= 80` 且答案错误时：

- 标记为高优先级；
- 记录用户原始回答；
- 不立即给完整长答案；
- 先让用户指出自己可能依赖的假设；
- 展示最小反证或边界证据；
- 进行一次结构相同、表面不同的辨别题；
- 下一次复查间隔不超过三天。

### 4.7 误解状态

```text
candidate
→ open
→ improving
→ resolved
```

可能从 `resolved` 回到：

```text
recurring
→ open
```

进入 `resolved` 的建议条件：

- 至少两个不同题面正确；
- 至少一次无提示；
- 用户能够解释原错误为什么具有迷惑性；
- 如果属于应用错误，至少完成一次正确近迁移；
- 证据发生在不同日期。

### 4.8 与知识库更新的边界

误解记录中的 correction 必须引用 Knowledge Core Evidence。

出现以下情况时，不继续纠正用户，而是创建知识核验候选：

- Evidence 自相矛盾；
- rubric 与当前 Wiki 内容不一致；
- 来源新版本改变了结论；
- 多次高质量回答被 Assessor 判错，但人工检查认为合理；
- 实际应用持续失败，且不能由情境差异解释。

学习系统只能创建：

```yaml
knowledge_update_candidate:
  reason: application_evidence_conflicts_with_current_claim
  related_claim_ids:
    - claim_x
  learning_event_ids:
    - evt_x
  requires_human_review: true
```

不能直接修改 Wiki。

---

## 5. 升级三：迁移与真实应用

### 5.1 为什么解释成功仍然不够

用户可能能够复述定义和机制，却无法：

- 在复杂案例中识别使用时机；
- 判断何时不适用；
- 在多个方法中作出选择；
- 调整方法以适应约束；
- 将知识迁移到表面不同的问题；
- 在真实行动后检查结果。

因此，迁移和应用必须成为独立学习证据。

### 5.2 迁移阶梯

```text
解释
→ 辨别
→ 近迁移
→ 远迁移
→ 真实应用
→ 应用复盘
```

#### 近迁移

保持问题结构相近，但改变：

- 数字；
-人物；
- 表述；
-部分条件；
-案例顺序。

目标是验证用户没有只记住原题答案。

#### 远迁移

保留底层结构，但改变：

- 领域；
- 表面线索；
- 任务形式；
- 可用资源；
- 约束条件。

目标是验证用户能识别知识的深层适用结构。

#### 真实应用

来自用户真实项目、写作、决策、工作流或行为实验。系统不能完全自动判断结果。

### 5.3 ApplicationChallenge

```yaml
schema_version: 1
challenge_id: challenge_review_plan_project
goal_id: goal_learning_plan_001
capability_id: cap_design_plan
type: real_project
status: active

context:
  project_id: project_personal_learning
  problem: 为未来一个月的学习材料设计复习计划
  constraints:
    - 每天最多二十分钟
    - 每周至少休息一天

required_reasoning:
  - 区分事实记忆和概念应用
  - 说明为什么选择当前间隔
  - 给出积压时的调整规则

knowledge_refs:
  note_ids:
    - concept_retrieval_practice
    - concept_spacing_effect
  evidence_ids:
    - ev_src01_v0001_0042

success_criteria:
  - 计划被实际执行至少一周
  - 用户确认计划可持续
  - 至少一次根据结果进行调整
```

### 5.4 ApplicationEvent

应用事件由用户确认：

```json
{
  "schema_version": 1,
  "event_id": "evt_application_01J...",
  "event_type": "application_recorded",
  "goal_id": "goal_learning_plan_001",
  "challenge_id": "challenge_review_plan_project",
  "occurred_at": "2026-09-20T18:00:00+08:00",
  "context": "为本月学习内容安排复习",
  "selected_knowledge": [
    "concept_retrieval_practice",
    "concept_spacing_effect"
  ],
  "reasoning_before_result": "先采用较短间隔，根据闭卷表现延长",
  "action_taken": "创建并执行一周复习计划",
  "outcome": "大部分任务完成，但周末发生积压",
  "user_assessment": "partial_success",
  "lessons": "每日上限需要按工作日和周末分别设置",
  "knowledge_update_candidate": false
}
```

### 5.5 应用前与应用后

应用前记录：

- 当前问题；
- 用户准备使用什么知识；
- 为什么适用；
- 哪些条件可能使它失效；
- 对结果的预测。

应用后记录：

- 实际行动；
- 结果；
- 预测是否成立；
- 失败来自知识错误、执行错误还是情境变化；
- 是否形成新的经验；
- 是否需要更新知识库。

必须先保存应用前判断，避免事后合理化。

### 5.6 项目集成

系统从 `50 Projects/` 提取候选学习机会，但只提出建议。

触发条件：

- 项目页面引用某个知识概念；
- 用户主动选择“将此知识用于当前项目”；
- 学习目标绑定了项目；
- 某项能力长期停留在 explain 或 discriminate；
- 项目出现与开放误解相关的决策。

建议形式：

```text
你已经两次解释“可逆决策”，但尚无应用证据。
当前项目包含一个可回退的技术选型。
是否创建一次应用挑战。
```

系统不得自动声称知识已经被应用。

### 5.7 应用结果分类

| 结果 | 含义 | 下一步 |
|---|---|---|
| `successful` | 正确选择并取得预期结果 | 增加真实应用证据 |
| `partial_success` | 方向正确但执行或边界处理不足 | 针对缺口生成练习 |
| `failed_execution` | 知识选择正确但执行失败 | 程序性练习或项目复盘 |
| `wrong_model` | 选择了不适合的知识 | 场景辨别 |
| `invalid_assumption` | 前置假设不成立 | 假设检查练习 |
| `knowledge_conflict` | 结果可能挑战现有结论 | 创建知识更新候选 |
| `inconclusive` | 结果不足以判断 | 保留记录，不升级能力 |

### 5.8 远迁移生成约束

Agent 生成远迁移任务时必须：

- 指定复用的底层结构；
- 改变至少两个表面维度；
- 不在题面中直接提示目标概念；
- 提供可验证的 rubric；
- 引用底层知识 Evidence；
- 说明任务是否存在多个合理答案；
- 不把单次开放式回答自动判定为完成。

---

## 6. Learning Orchestrator

### 6.1 决策职责

Orchestrator 不负责生成所有自然语言内容。它负责确定性地决定：

- 当前目标是否需要诊断；
- 哪项能力缺少证据；
- 是否存在优先误解；
- 下一活动属于回忆、解释、辨别还是迁移；
- 是否应该结束本次会话；
- 是否安排延迟复查；
- 知识版本更新是否使旧学习对象需要复核。

Agent 负责在指定活动类型和 Evidence 上生成题目、提示和反馈。

### 6.2 下一活动选择顺序

```text
1. 知识版本是否过期或冲突
2. 是否存在高置信度开放误解
3. 目标必需能力是否缺少基础证据
4. 是否需要从解释升级到辨别
5. 是否需要近迁移
6. 是否存在真实项目应用机会
7. 是否到达延迟复习时间
8. 是否应该暂停或结束
```

该顺序第一版固定实现，不引入不透明推荐模型。

### 6.3 会话预算

一次默认会话：

- 一个主要能力；
- 5–12 分钟目标时长；
- 最多三个主要活动；
- 最多两级提示；
- 最多一个新误解确认；
- 达到本次证据目标后立即结束。

结束原因必须可见：

```text
evidence_reached
time_budget_reached
needs_source_review
needs_human_judgment
user_stopped
knowledge_conflict
```

### 6.4 活动选择与错误类型映射

| 错误类型 | 首选活动 | 第二选择 |
|---|---|---|
| fact_gap | 自由回忆 | 提示递减 |
| causal_gap | 因果链解释 | teach-back |
| boundary_error | 反例 | 条件变化 |
| concept_confusion | 对比案例 | 交错练习 |
| proxy_confusion | 指标反例 | 预测结果 |
| overgeneralization | 范围判断 | 失败案例 |
| unsupported_inference | 证据分类 | 来源核验 |
| procedure_error | 实际执行 | 步骤排序 |
| application_mismatch | 场景选择 | 决策树 |
| outdated_knowledge | 新旧差异 | 当前版本应用 |

### 6.5 知识版本联动

LearningObject 必须保存：

```yaml
knowledge_snapshot:
  note_id: concept_retrieval_practice
  note_hash: sha256:...
  evidence:
    - evidence_id: ev_src01_v0001_0042
      source_id: src_01J...
      version_id: v0001
      span_hash: sha256:...
```

当关联知识更新时：

- 历史 Attempt 仍引用旧快照；
- LearningObject 标记为 `needs_review`；
- 目标如果依赖该能力，进入 `needs_revalidation`；
- 系统优先生成变化辨别任务；
- 不自动把历史正确答案改成错误；
- 不自动删除旧的真实应用证据；
- 用户重新验证当前版本后恢复 active 或 achieved。

---

## 7. 持久化设计

### 7.1 Vault 目录增量

在现有 `40 Learning/` 下增加：

```text
40 Learning/
├── Goals/
│   └── {goal_id}.yaml
├── Objects/
│   └── {learning_object_id}.yaml
├── Challenges/
│   └── {challenge_id}.yaml
├── Misconceptions/
│   └── {misconception_id}.yaml
├── Events/
│   └── YYYY-MM/
│       └── {session_id}.jsonl
└── Views/
    ├── Active Goals.md
    ├── Open Misconceptions.md
    └── Application Opportunities.md
```

`Views/` 中的 Markdown 可以重建，不是事实源。

### 7.2 SQLite 派生表

```text
learning_goals
goal_capabilities
capability_evidence
misconceptions
misconception_occurrences
application_challenges
learning_events
knowledge_snapshot_refs
review_queue
```

删除 SQLite 后必须从 YAML 和 JSONL 完整重建。

### 7.3 事件类型

第一版新增：

```text
goal_created
goal_activated
goal_paused
diagnostic_started
attempt_submitted
confidence_recorded
hint_used
feedback_recorded
misconception_proposed
misconception_confirmed
misconception_state_changed
capability_evidence_added
challenge_created
application_prediction_recorded
application_outcome_recorded
knowledge_revalidation_required
goal_achieved
```

事件使用全局唯一 `event_id`，追加写入，不原地修改。

---

## 8. MCP、CLI 与 UI

### 8.1 MCP 增量工具

```text
lw_create_goal
lw_get_goal
lw_start_diagnostic
lw_get_next_activity
lw_submit_attempt
lw_submit_confidence
lw_submit_feedback
lw_propose_misconception
lw_update_misconception
lw_create_application_challenge
lw_record_application_prediction
lw_record_application_outcome
lw_get_capability_evidence
lw_list_revalidation_tasks
```

权限约束：

- Agent 可以提议目标拆分和误解；
- Agent 不能伪造用户回答；
- Agent 不能自动确认真实应用成功；
- Agent 不能直接把 Goal 标记为 achieved；
- achieved 由确定性规则根据事件证据派生；
- 学习结果不能直接修改 Wiki。

### 8.2 CLI

```bash
lw goal create
lw goal list
lw goal show GOAL_ID
lw goal pause GOAL_ID

lw diagnostic start GOAL_ID
lw learn next GOAL_ID
lw learn attempt SESSION_ID

lw misconception list
lw misconception show MISCONCEPTION_ID
lw misconception resolve MISCONCEPTION_ID

lw challenge create GOAL_ID
lw challenge show CHALLENGE_ID
lw application record CHALLENGE_ID

lw learning revalidate
lw learning rebuild
```

### 8.3 Obsidian 界面

#### Goal View

- 当前能力目标；
- 每项能力要求层级；
- 当前最高证据；
- 缺少的证据；
- 关联知识和项目；
- 下一推荐活动；
- 暂停和结束目标。

#### Diagnostic Modal

- 开放回答；
- 辨别案例；
- 应用判断；
- 置信度；
- 不显示完整标准答案。

#### Misconception View

- 用户原始错误信念；
- 错误类型；
- 触发情境；
- 置信度；
- 使用过的干预；
- 是否复发；
- 纠正 Evidence；
- 下一辨别任务。

#### Transfer View

- 近迁移和远迁移任务；
- 当前项目应用机会；
- 应用前判断；
- 应用后结果；
- 能力证据变化；
- 知识更新候选。

#### Session View

- 本次目标；
- 剩余活动预算；
- 当前活动；
- 用户回答；
- 提示；
- Evidence 反馈；
- 本次获得的能力证据；
- 明确结束原因。

---

## 9. 评估与可观测性

### 9.1 不使用的核心指标

- 学习时长；
- 连续打卡；
- 总题目数；
- AI 生成内容数；
- Wiki 页数；
- 单一 mastery 百分比。

### 9.2 核心指标

#### 能力形成

- 达到目标层级的能力比例；
- 从目标创建到获得证据的时间；
- 跳过已掌握内容的比例；
- 目标暂停或放弃原因；
- 证据过期后重新验证成功率。

#### 误解纠正

- 高置信度错误数量；
- 同一误解复发率；
- 不同活动类型的纠正效果；
- 从 open 到 resolved 的时间；
- resolved 后再次 recurring 的比例。

#### 迁移与应用

- 解释成功后近迁移成功率；
- 近迁移到远迁移的下降幅度；
- 真实应用尝试数量；
- 用户确认的成功、部分成功和失败比例；
- 应用结果触发知识更新候选的数量。

#### 元认知

- 置信度与正确性的校准误差；
- 正确但低置信度的比例；
- 高置信度错误集中在哪些知识类型；
- 用户对自己能力判断与延迟表现的偏差。

### 9.3 成功不是全部达到最高层级

不同知识允许不同结束标准：

- 临时事实：recall；
- 基础概念：explain；
- 容易混淆的模型：discriminate；
- 工作方法：near_transfer 或 real_application；
- 核心专业能力：far_transfer 和多次真实应用。

系统应避免把所有内容推向最高层级。

---

## 10. 测试策略

### 10.1 单元测试

- Goal 状态转换；
- 能力层级比较；
- 完成策略；
- 诊断路由；
- 错误类型到活动类型映射；
- 高置信度错误规则；
- 误解确认与解决条件；
- 迁移层级升级；
- 应用结果分类；
- Knowledge Snapshot 过期检测；
- 事件重放和幂等。

### 10.2 集成测试

- 创建 Goal 后可以完成诊断并得到下一活动；
- 已有 explain 证据时不会重复安排 recall；
- 高置信度错误创建高优先级误解候选；
- 同一误解第二次出现后被确认；
- direct_explanation 无效后不会继续重复相同活动；
- 近迁移成功不会自动升级为 real_application；
- 真实应用必须有用户确认；
- Goal 只有满足 completion policy 才进入 achieved；
- Knowledge Note 更新后相关 LearningObject 进入 needs_review；
- 删除 SQLite 后可以恢复 Goal、误解和应用证据。

### 10.3 对抗测试

1. Agent 试图直接把 Goal 标记 achieved，服务拒绝。
2. Agent 在用户未回答时提交伪造 Attempt，服务拒绝缺少用户交互上下文的记录。
3. 同一题重复三次答对，不得自动算远迁移。
4. 一个真实项目文件引用 Wiki，不得自动算应用成功。
5. 用户应用失败，不得自动认定知识库错误。
6. Assessor 与 Knowledge Evidence 冲突时，进入人工核验而不是强判用户错误。
7. 来源版本更新后，旧 Attempt 不被改写。
8. 远迁移题面直接透露目标概念时，黄金测试失败。
9. 达到时间预算后，系统停止继续生成活动。
10. 低价值目标被用户暂停后，不继续产生复习债务。

### 10.4 学习效果试点

选择三类真实目标：

- 一个概念解释目标；
- 一个相似概念辨别目标；
- 一个真实项目应用目标。

运行四周，比较：

- 起点诊断；
- 即时表现；
- 七天后闭卷表现；
- 二十一天后迁移表现；
- 实际项目结果；
- 用户每周投入时间；
- 用户认为无价值或负担过高的活动。

系统设计是否成功必须同时考虑学习效果和维护成本。

---

## 11. 分阶段实施

### L1：能力目标与诊断

实现：

- Goal、Capability 和 CompletionPolicy；
- Goal View；
- 最小诊断；
- 确定性下一活动路由；
- 能力证据派生。

验收：

- 一个真实目标可以被拆成不超过五项能力；
- 诊断能够跳过已有充分证据的活动；
- achieved 有可解释证据；
- 不出现总 mastery 百分比。

### L2：误解模型

实现：

- 十种首版错误类型；
- Misconception 对象和状态；
- 高置信度错误；
- 干预历史；
- 错误类型到活动映射。

验收：

- 重复错误能够识别为同一误解；
- 失败的干预不会被无限重复；
- resolved 需要不同情境和不同日期的证据；
- 误解不会直接修改 Wiki。

### L3：迁移与真实应用

实现：

- Transfer Level；
- ApplicationChallenge；
- 应用前预测；
- 应用后复盘；
- Project 候选机会；
- Knowledge Update Candidate。

验收：

- 近迁移和远迁移使用不同证据；
- 文件引用不等于应用成功；
- 真实应用由用户确认；
- 应用失败能区分知识、选择、执行和情境问题。

### L4：联动与试点

实现：

- Knowledge Snapshot；
- 知识更新后的再验证；
- 三类指标 Dashboard；
- 四周试点工具和导出报告。

验收：

- 知识更新不篡改历史学习记录；
- 受影响目标能够进入 needs_revalidation；
- 用户可以看到系统为何推荐当前活动；
- 四周试点可以评价学习收益与维护成本。

---

## 12. Definition of Done

三项升级同时满足以下条件才算完成：

- 用户能够以可观察行为定义能力目标；
- 每个目标有明确的完成策略；
- 起点诊断可以跳过已有充分证据的内容；
- 学习活动由能力缺口和误解驱动；
- 高置信度错误得到优先处理；
- 误解包含原信念、错误类型、触发情境和干预历史；
- 系统不会无限重复已经失败的纠正方式；
- 回忆、解释、辨别、近迁移、远迁移和真实应用证据彼此分开；
- 真实应用必须记录应用前判断和应用后结果；
- 应用结果必须由用户确认；
- 学习结果只能创建知识更新候选，不能直接修改 Wiki；
- 学习对象绑定具体知识版本和 Evidence；
- 知识更新后可以定位需要重新验证的目标；
- 历史回答、反馈和应用记录保持不可变；
- 删除派生数据库后可以从 YAML 和 JSONL 重建；
- 默认会话有明确活动和时间预算；
- 系统能够说明为何推荐下一项活动；
- 三类真实目标经过至少四周试点。

---

## 13. 最终产品判断

三项升级后的学习系统不再以“今天应该复习哪张卡”为中心，而以三个问题为中心：

1. 为了真实目标，用户还缺少哪项能力证据；
2. 阻止能力形成的核心误解是什么；
3. 用户是否能在新情境和真实行动中正确使用知识。

最值得优先实现的顺序是：

```text
能力目标与诊断
→ 误解模型
→ 近迁移
→ 真实应用
→ 远迁移与知识更新联动
```

原因是：

- 没有能力目标，误解和迁移都缺少方向；
- 没有误解模型，个性化只会退化为调节题目难度；
- 没有迁移和真实应用，系统只能证明用户记住了内容，不能证明用户获得了能力。

最终优化目标是：

> 用最少但最有诊断价值的学习活动，帮助用户形成能够在新问题和真实项目中独立调用的能力。

