# 学习机制使用说明

> 面向使用者的操作手册：如何用 `lw` CLI 与 MCP 工具完成「能力目标 → 诊断 → 练习 → 误解纠正 → 迁移与真实应用」的完整闭环。
> 对应实现见 `learning-wiki` 仓库 M3（学习机制升级思路 §3–§8）。
>
> 适用版本：M3 学习机制升级（L1–L3 + 知识快照联动）

---

## 0. 30 秒上手

```bash
# 安装工具（首次）
cd ~/Documents/knowledge/learning-wiki && make install

# 在你的 Vault 上初始化（幂等，不动既有笔记）
uv run lw init-vault ~/Documents/knowledge/AI-Knowledge

# 别名（建议写入 ~/.zshrc）
alias lwai='uv run --project ~/Documents/knowledge/learning-wiki lw --vault ~/Documents/knowledge/AI-Knowledge'

# 最小闭环
lwai goal create --title "掌握检索练习" \
  --capability "cap_explain|能解释检索练习为何有效|explain" \
  --note-id concept_retrieval_practice --activate
lwai goal show <GOAL_ID>          # 看能力证据与缺口（没有 mastery 百分比）
lwai diagnostic start <GOAL_ID>    # 起点诊断
lwai learn next <GOAL_ID>         # 系统推荐下一活动（含为何推荐）
```

未指定 `--vault` 时按 `LW_VAULT` 环境变量或当前目录解析。

---

## 1. 核心概念

### 1.1 两类分离的数据

系统严格区分两种数据，**永远不混用**：

| 类别 | 内容 | 事实源 |
|---|---|---|
| **知识状态** | 来源、证据、Wiki、争议、版本、时效 | `10 Sources/`、`30 Wiki/` |
| **学习者状态** | 目标、尝试、误解、迁移证据、应用结果 | `40 Learning/` |

> 答错通常意味着**学习者需要纠正**，不意味着知识库错误。学习结果只能生成「知识更新候选」，**不能直接修改 Wiki**。

### 1.2 能力层级（不是总分）

系统用 8 个离散层级描述能力，**不输出单一掌握百分比**：

| 层级 | 含义 |
|---|---|
| `unseen` | 尚无学习记录 |
| `recognize` | 能识别术语/正确选项 |
| `recall` | 能闭卷回忆关键内容 |
| `explain` | 能解释机制、原因和边界 |
| `discriminate` | 能与相似概念正确区分 |
| `near_transfer` | 能处理与学习案例相似的新问题 |
| `far_transfer` | 能处理表面不同但结构相同的问题 |
| `real_application` | 在真实项目中正确使用 |

> 能力状态只表示「当前存在的最高证据」，**不表示永久掌握**；证据会随时间过期（默认 90 天）。

### 1.3 证据派生规则（确定性）

一次反馈能产生的能力证据层级：

| 结果 | 条件 | 证据层级 |
|---|---|---|
| `successful` | 无提示、闭卷 | 活动对应层级 |
| `successful` | 用了提示 / 开卷 | 降一级（最低 recognize） |
| `partial` | 无提示 | 降两级 |
| `partial` | 用了提示 | 降三级 |
| `failed` | — | 不产生证据 |

> 同一题重复答对三次，**不会**自动算远迁移——远迁移证据必须来自 `far_transfer` 活动且通过 rubric。

### 1.4 一次会话的预算

| 约束 | 上限 |
|---|---|
| 主要能力目标 | 1 个 |
| 主要活动 | 3 个 |
| 提示层级 | 2 级 |
| 新误解确认 | 1 个 |

达到证据目标或预算即结束，结束原因可见：`evidence_reached` / `time_budget_reached` / `needs_source_review` / `needs_human_judgment` / `user_stopped` / `knowledge_conflict`。

---

## 2. 目录结构

初始化后 `40 Learning/` 下会创建：

```text
40 Learning/
├── Goals/{goal_id}.yaml          # 能力目标（事实源）
├── Objects/{learning_object_id}.yaml   # 学习对象（题面+rubric+知识快照）
├── Challenges/{challenge_id}.yaml      # 应用挑战
├── Misconceptions/{misconception_id}.yaml  # 误解记录
├── Events/
│   └── YYYY-MM/
│       └── {session_id}.jsonl     # 学习事件（append-only，按 event_id 幂等）
└── Views/                         # 可重建的 Markdown 视图（非事实源）
    ├── Active Goals.md
    ├── Open Misconceptions.md
    └── Application Opportunities.md
```

- `Events/` 下的事件**不可原地修改**；纠正、反馈、复评都通过追加事件。
- 派生状态（SQLite、能力层级、Views）可随时删除，`lw rebuild` 从 YAML + JSONL 完整重建。

---

## 3. 能力目标（Goal）

### 3.1 创建目标

能力以**可观察行为**定义，格式为 `capability_id|行为描述|required_level`：

```bash
lwai goal create \
  --title "为不同知识设计合适的复习计划" \
  --target-date 2026-10-01 \
  --importance high \
  --capability "cap_explain|能解释检索练习为何改善长期保持|explain" \
  --capability "cap_discriminate|能根据案例区分重读、检索与交错|discriminate" \
  --capability "cap_design|能为真实材料设计复习计划|real_application" \
  --note-id concept_retrieval_practice \
  --note-id concept_spacing_effect \
  --evidence-id ev_src01_v0001_0042 \
  --project-id project_personal_learning \
  --motivation "为自己的长期学习建立可持续安排" \
  --activate
```

参数说明：

| 参数 | 说明 |
|---|---|
| `--capability` | **可重复**。`capability_id\|行为\|层级`，层级可省略（默认 explain） |
| `--note-id` | 知识依赖的 Wiki 笔记 ID（可重复）；笔记更新时触发再验证 |
| `--evidence-id` | 绑定的 Evidence（可重复） |
| `--target-date` / `--importance` | 目标日期 / low·medium·high |
| `--project-id` / `--motivation` | 关联项目 / 动机（可重复 project-id） |
| `--activate` | 创建后直接激活（draft → active） |

> **超过 5 项能力**时系统返回拆分建议：每个目标应能在一至四周内验证、每项能力可被一个明确活动检验。不为每个知识点创建目标。

### 3.2 查看 / 管理

```bash
lwai goal list [--status active]      # 列出目标
lwai goal show GOAL_ID                # 能力证据与缺口（无 mastery 百分比）
lwai goal activate GOAL_ID            # 激活（draft → active）
lwai goal pause GOAL_ID               # 暂停：不再产生复习债务
lwai goal abandon GOAL_ID             # 放弃
```

`goal show` 输出每项能力的「要求层级 / 当前最高证据」与未达成的具体原因（超龄 / 依赖提示 / 非闭卷 / 缺用户确认应用）。

> 目标状态机：`draft → active → diagnosing → learning → transfer_testing → application_pending → achieved`，旁路 `paused / abandoned / needs_revalidation`。
> **`achieved` 不能手动设置**——只能由确定性规则按完成策略派生（见 §7）。

---

## 4. 起点诊断

新目标开始时做最小诊断，定位最小知识缺口：

```bash
lwai diagnostic start GOAL_ID
# 输出：诊断会话 ID + 三类活动框架
#   - recall: 自由回忆
#   - discriminate: 概念辨别
#   - apply: 应用判断
```

- 已有充分证据的低层活动会被**自动跳过**（不浪费练习）。
- 诊断**不输出通过/失败总结果**，只用于选择下一步。
- Agent 负责在框架上生成具体题面（用 `lw learn object-create`）。

```bash
lwai diagnostic complete SESSION_ID --summary '{"weakest_capability":"cap_explain"}'
lwai diagnostic summary SESSION_ID      # 查看诊断摘要（无通过/失败）
```

---

## 5. 学习活动闭环

### 5.1 获取下一活动

```bash
lwai learn next GOAL_ID
```

输出包含：活动类型、关联能力/误解、**reason（为何推荐）**、剩余预算、生成约束。系统按固定顺序路由（§6.2）：

1. 知识版本是否过期/冲突 → 提示再验证
2. 是否存在高置信度开放误解 → 优先安排干预
3. 必需能力缺少基础证据 → 按缺口路由
4. 是否需从解释升级到辨别
5. 是否需要近迁移
6. 是否存在真实项目应用机会
7. 是否到达延迟复习时间
8. 是否应暂停或结束

> 该顺序第一版固定实现，**不引入不透明推荐模型**。

### 5.2 创建学习对象

Agent/用户在系统指定的活动类型上生成具体题面：

```bash
lwai learn object-create \
  --session SESSION_ID \
  --goal GOAL_ID \
  --title "解释检索练习" \
  --prompt "请闭卷解释检索练习为何有效" \
  --type explain \
  --capability cap_explain \
  --rubric "说明主动提取" \
  --rubric "提到长期保持" \
  --note concept_retrieval_practice    # 绑定 Wiki 笔记 → 自动建知识快照
```

`--type` 取值：`recall / explain / discriminate / apply / near_transfer / far_transfer / real_application`。

绑定 `--note` 时系统自动从 Wiki 笔记当前版本构建**知识快照**（note_hash + Evidence 四元组），用于后续过期检测。

### 5.3 闭卷作答（先于反馈）

```bash
lwai learn attempt SESSION_ID LEARNING_OBJECT_ID \
  --response "重读让记忆更流畅所以更牢固" \
  --confidence 90 \
  --hints 0 \
  # --source-opened     # 仅当用户确实打开过来源时才加
```

护栏：
- **回答为空即拒绝**（防止伪造作答）。
- **会话必须真实存在**（来自 `learn next` / `diagnostic start`），否则拒绝。
- `--confidence` 0–100，用于元认知校准；高置信度（≥80）答错会被优先处理。
- `--hints` 超过 2 级即拒绝（会话预算）。

### 5.4 记录反馈（后于作答）

```bash
lwai learn feedback SESSION_ID LEARNING_OBJECT_ID \
  --result successful \
  --rubric '[{"item":"说明主动提取","met":true},{"item":"提到长期保持","met":true}]' \
  --feedback "机制解释完整"
```

- `--result`：`successful / partial / failed`。
- **反馈必须在作答之后落盘**（先作答后反馈的确定性保证）。
- 反馈后系统自动：派生能力证据、安排延迟复习、评估是否达成目标。
- 若目标满足完成策略，会自动进入 `achieved` 并落 `goal_achieved` 事件。

---

## 6. 误解模型

### 6.1 记录一次错误观察

```bash
lwai misconception propose \
  --concept concept_retrieval_practice \
  --belief "重读越流畅，长期记忆效果通常越好" \
  --correction "学习时的主观流畅性不能单独预测延迟保持" \
  --type proxy_confusion \
  --evidence ev_src01_v0001_0042 \
  --context "选择复习方法" \
  --confidence 90
```

`--type` 为 10 种封闭错误类型之一（首版封闭，不可随意新增）：

| 类型 | 描述 |
|---|---|
| `fact_gap` | 缺少必要事实 |
| `causal_gap` | 记得结论但无法解释机制 |
| `boundary_error` | 忽略适用条件和边界 |
| `concept_confusion` | 混淆相似概念 |
| `proxy_confusion` | 把替代指标误当目标 |
| `overgeneralization` | 将局部结论推广过度 |
| `unsupported_inference` | 推断没有证据支持 |
| `procedure_error` | 步骤顺序或条件错误 |
| `application_mismatch` | 选择了不适合当前情境的方法 |
| `outdated_knowledge` | 使用了已更新的旧结论 |

### 6.2 候选 vs 确认

| 情况 | 结果 |
|---|---|
| 一次普通错误（低置信度） | 仅建**候选**（candidate） |
| 一次高置信度（≥80）答错 | 直接**确认**（open，高优先级） |
| 同一错误结构出现两次 | 确认为 open |
| 用户明确确认原持有该信念 | 确认 |
| 真实应用中因此错误决策 | 确认 |
| resolved 后再次出现 | 转 recurring → open |

> AI 提议错误类型，确定性服务保存结构；用户可修改或拒绝。

### 6.3 查看 / 干预 / 解决

```bash
lwai misconception list [--status open]
lwai misconception show MISCONCEPTION_ID   # 含解决证据统计
lwai misconception evidence MISCONCEPTION_ID   # 不同题面正确/无提示/不同日期计数
lwai misconception intervene MISCONCEPTION_ID --activity contrast_case --result recurred
lwai misconception resolve MISCONCEPTION_ID           # 需满足 §4.7 条件
lwai misconception resolve MISCONCEPTION_ID --force --reason "用户已确认纠正"  # 强制解决（留痕）
```

解决条件（§4.7）：
- 至少两个**不同题面**正确
- 至少一次**无提示**
- 证据发生在**不同日期**

**失败的首选干预不会被无限重复**：若 `contrast_case` 失败（recurred），下次路由自动选第二选择 `interleaved_practice`。

### 6.4 高置信度错误流程（§4.6）

当 `confidence ≥ 80` 且答错时：
- 标记高优先级，**复查间隔不超过 3 天**
- 不立即给完整长答案
- 先让用户指出自己可能依赖的假设
- 展示最小反证或边界证据
- 进行一次结构相同、表面不同的辨别题

### 6.5 与知识库的边界

当出现以下情况时，系统**不继续纠正用户**，而是创建知识更新候选（人工核验，不改 Wiki）：
- Evidence 自相矛盾
- rubric 与当前 Wiki 内容不一致
- 来源新版本改变了结论
- 多次高质量回答被 Assessor 判错但人工认为合理
- 实际应用持续失败且不能由情境差异解释

---

## 7. 迁移与真实应用

迁移和应用是**独立的学习证据**，不能靠「能复述定义」替代。

### 7.1 迁移阶梯

```text
解释 → 辨别 → 近迁移 → 远迁移 → 真实应用 → 应用复盘
```

- **近迁移**：保持结构相近，改数字/表述/部分条件（验证没只记住原题答案）
- **远迁移**：改变领域/表面线索/任务形式（验证能识别深层结构）
- **真实应用**：来自真实项目/写作/决策

> Agent 生成远迁移任务时必须：复用底层结构、改变≥2 个表面维度、不在题面透露目标概念、提供可验证 rubric、引用底层 Evidence、说明是否有多合理答案、不把单次开放式回答自动判完成。

### 7.2 创建应用挑战

```bash
lwai challenge create GOAL_ID \
  --capability cap_design \
  --problem "为未来一个月的学习材料设计复习计划" \
  --type real_project \
  --project project_personal_learning \
  --constraint "每天最多二十分钟" \
  --constraint "每周至少休息一天" \
  --reasoning "区分事实记忆和概念应用" \
  --reasoning "说明为什么选择当前间隔" \
  --note concept_retrieval_practice \
  --criterion "计划被实际执行至少一周" \
  --criterion "用户确认计划可持续"
```

`--type`：`real_project / near_transfer_task / far_transfer_task`。

```bash
lwai challenge list [--goal GOAL_ID]
lwai challenge show CHALLENGE_ID
lwai challenge opportunities GOAL_ID    # 从 50 Projects 提取候选应用机会（仅建议）
```

> **项目文件引用 Wiki 不等于应用成功**——`opportunities` 只给建议，系统不自动认定。

### 7.3 应用前预测（必须先于结果）

```bash
lwai application predict CHALLENGE_ID \
  --context "为本月学习内容安排复习" \
  --knowledge concept_retrieval_practice \
  --knowledge concept_spacing_effect \
  --reasoning "先采用较短间隔，根据闭卷表现延长" \
  --prediction "一周内可持续"
```

**必须先保存应用前判断，再记录结果**，避免事后合理化。已记录结果后无法再补预测。

### 7.4 应用后结果（用户确认）

```bash
lwai application record CHALLENGE_ID \
  --action "创建并执行一周复习计划" \
  --outcome "大部分任务完成，但周末发生积压" \
  --assessment partial_success \
  --lessons "每日上限需要按工作日和周末分别设置"
```

- **必须在终端（TTY）执行并二次确认**；非 TTY（脚本/管道）会被拒绝。
- `--assessment` 取 7 类结果之一：

| 结果 | 含义 | 下一步 |
|---|---|---|
| `successful` | 正确选择并取得预期结果 | 增加真实应用证据 |
| `partial_success` | 方向正确但执行/边界不足 | 针对缺口生成练习 |
| `failed_execution` | 选择正确但执行失败 | 程序性练习或项目复盘 |
| `wrong_model` | 选择了不适合的知识 | 场景辨别练习 |
| `invalid_assumption` | 前置假设不成立 | 假设检查练习 |
| `knowledge_conflict` | 结果可能挑战现有结论 | **创建知识更新候选**（不改 Wiki） |
| `inconclusive` | 结果不足以判断 | 保留记录，不升级能力 |

```bash
lwai application show CHALLENGE_ID   # 查看应用前/后事件
```

---

## 8. 目标完成与达成

### 8.1 完成策略（CompletionPolicy）

`achieved` 的进入由确定性规则评估，**不可手动设置**。默认必要条件：

1. 每个必需能力达到目标层级（证据未超龄、闭卷、不依赖提示）
2. 高层能力没有完全依赖提示
3. 要求真实应用的目标，已有**用户确认**的 `successful` 应用事件
4. 相关知识版本未被标记过期/争议

```bash
lwai goal show GOAL_ID   # 显示每条不满足的原因
```

满足时系统自动转 `achieved` 并落 `goal_achieved` 事件。

### 8.2 不同知识允许不同结束标准

系统**不把所有内容推向最高层级**：

| 知识类型 | 合理结束层级 |
|---|---|
| 临时事实 | `recall` |
| 基础概念 | `explain` |
| 容易混淆的模型 | `discriminate` |
| 工作方法 | `near_transfer` 或 `real_application` |
| 核心专业能力 | `far_transfer` + 多次真实应用 |

完成策略可按目标定制（通过 MCP `lw_create_goal` 的 completion_policy 或直接编辑 YAML）。

---

## 9. 知识更新联动

学习对象绑定**知识快照**（创建时的 note_hash + Evidence 四元组）。当关联的 Wiki 笔记更新时：

```bash
lwai learning revalidate
# 检查对象 / 过期对象；过期 → needs_review，目标 → needs_revalidation
# 历史回答不会被改写（只追加 knowledge_revalidation_required 事件）

lwai learning mark-reviewed LEARNING_OBJECT_ID   # 用户复核后恢复 active
lwai learning revalidate-tasks                   # 列出需要重新验证的目标
lwai learning objects [--goal GOAL_ID]           # 列出学习对象（含 needs_review）
```

> 来源/笔记版本更新后，旧 Attempt **仍引用旧快照**，不被改写；用户重新验证当前版本后恢复。

---

## 10. Views 视图（Obsidian）

`40 Learning/Views/` 下的 Markdown 是**派生物**（可随时重建，不是事实源），可在 Obsidian 中直接打开：

- **Active Goals.md** — 进行中目标、每项能力的当前证据与缺口
- **Open Misconceptions.md** — 开放误解、原信念、错误类型、干预历史
- **Application Opportunities.md** — 应用挑战与机会

```bash
lwai learning views     # 手动重建视图
lwai rebuild           # 全量重建后也会自动重建视图
```

状态变更（创建目标、记录反馈、记录应用结果等）后视图会自动重建。

---

## 11. 删库重建

派生状态（SQLite、能力层级、Views）可随时删除，从 YAML + JSONL 完整重建：

```bash
rm -rf ~/Documents/knowledge/AI-Knowledge/.learning-wiki
lwai rebuild
# 输出：来源/版本/证据/Wiki/学习对象/事件/目标/误解/挑战/提案/操作记录 数量
```

重建范围：来源与版本、Evidence、Wiki、学习对象/事件、目标、误解、挑战、提案、操作记录、FTS5、能力证据与当前层级、复习队列、Views。不恢复：瞬时任务、UI 状态、缓存。

---

## 12. MCP 工具（给 Agent 用）

注册到 Claudian / Claude Code（vault 的 `.claude/mcp.json`）：

```json
{
  "mcpServers": {
    "learning-wiki": {
      "command": "<repo>/.venv/bin/lw",
      "args": ["mcp", "--vault", "/path/to/Vault"]
    }
  }
}
```

学习类工具（§8.1）：

| 工具 | 作用 |
|---|---|
| `lw_create_goal` | 创建目标（>5 能力返回拆分建议） |
| `lw_get_goal` | 查看目标能力证据与缺口 |
| `lw_start_diagnostic` | 起点诊断 |
| `lw_get_next_activity` | 下一活动（确定性路由 + reason） |
| `lw_submit_attempt` | 提交用户闭卷回答 |
| `lw_submit_confidence` | 补记 0–100 置信度 |
| `lw_submit_feedback` | 记录评估反馈 |
| `lw_propose_misconception` | 提议误解（高置信度自动确认） |
| `lw_update_misconception` | 记录干预结果 |
| `lw_create_application_challenge` | 创建应用挑战 |
| `lw_record_application_prediction` | 应用前预测 |
| `lw_record_application_outcome` | 应用后结果（须 `user_confirmed=true`） |
| `lw_get_capability_evidence` | 能力证据（事件级，可回溯） |
| `lw_list_revalidation_tasks` | 需重新验证的目标 |
| `lw_activate_goal` | 激活目标 |
| `lw_resolve_misconception` | 解决误解（须用户确认） |

**权限护栏**：

- ✅ Agent 可以提议目标拆分和误解、生成题面与反馈
- ❌ Agent 不能伪造用户回答（attempt 必须来自真实会话且带非空回答）
- ❌ Agent 不能自动确认真实应用成功（outcome 必须 `user_confirmed=true`）
- ❌ Agent 不能直接把 Goal 标记为 achieved（无此工具，只能由规则派生）
- ❌ 学习结果不能直接修改 Wiki（只能创建知识更新候选）

---

## 13. 完整工作流示例

```bash
# 1. 创建并激活目标（绑定知识依赖）
lwai goal create --title "掌握检索练习" \
  --capability "cap_explain|能解释检索练习为何有效|explain" \
  --capability "cap_discriminate|能区分重读/检索/交错|discriminate" \
  --note-id concept_retrieval_practice --activate

# 2. 诊断 → 学习
lwai diagnostic start GOAL_ID
lwai diagnostic complete SESSION_ID --summary '{}'

# 3. 第一轮：闭卷作答 + 反馈
NEXT=$(lwai learn next GOAL_ID)         # 系统推荐 recall
lwai learn object-create --session S --goal GOAL_ID --title "自由回忆" \
  --prompt "闭卷说出检索练习核心" --type recall --capability cap_explain
lwai learn attempt S OBJ --response "主动提取比重读更牢固" --confidence 60
lwai learn feedback S OBJ --result successful

# 4. 升级到辨别（系统自动路由，不重复 recall）
lwai learn next GOAL_ID                  # → discriminate

# 5. 若高置信度答错 → 误解自动确认
lwai learn attempt S OBJ2 --response "重读越流畅越好" --confidence 90
lwai learn feedback S OBJ2 --result failed
lwai misconception propose --concept concept_retrieval_practice \
  --belief "重读越流畅越好" --correction "流畅性不预测延迟保持" \
  --type proxy_confusion --confidence 90   # → open（高优先级）
lwai learn next GOAL_ID                   # → 优先安排误解干预

# 6. 近迁移成功不会自动升级为真实应用
lwai learn object-create ... --type near_transfer
lwai learn attempt ...; lwai learn feedback ... --result successful

# 7. 真实应用（先预测后结果，用户确认）
lwai challenge create GOAL_ID --capability cap_discriminate \
  --problem "为本月学习设计复习计划" --criterion "执行至少一周"
lwai application predict CH --context ... --reasoning ... --prediction ...
lwai application record CH --action ... --outcome ... --assessment successful

# 8. 知识更新联动
lwai learning revalidate                  # 笔记更新后检测过期

# 9. 随时删库重建
rm -rf .learning-wiki && lwai rebuild
```

---

## 14. 评估指标（系统关注什么）

**不使用的指标**：学习时长、连续打卡、总题目数、AI 生成内容数、Wiki 页数、单一 mastery 百分比。

**核心指标**：
- 达到目标层级的能力比例、目标暂停/放弃原因
- 高置信度错误数量、同一误解复发率、不同活动类型的纠正效果
- 解释成功后近迁移成功率、近迁移到远迁移的下降幅度
- 用户确认的成功/部分成功/失败比例、应用结果触发知识更新候选的数量
- 置信度与正确性的校准误差、高置信度错误集中在哪些知识类型

> 成功不是「全部达到最高层级」，而是用最少但最有诊断价值的学习活动，帮助形成能在新问题和真实项目中独立调用的能力。

---

## 附：常见问题

**Q：为什么 `lw learn attempt` 报「会话不存在」？**
A：作答必须来自 `lw learn next` 或 `lw diagnostic start` 建立的会话。系统据此拒绝 Agent 在用户未回答时伪造记录。

**Q：为什么 `lw application record` 报「非 TTY 拒绝」？**
A：应用结果必须由用户在终端确认。脚本/管道环境无法满足，请改用交互式终端，或经 MCP 传 `user_confirmed=true`。

**Q：为什么回答对了一次，目标还没 achieved？**
A：可能因为：(1) 该能力要求更高层级；(2) 证据依赖了提示或开卷；(3) 证据超龄；(4) 要求真实应用但缺用户确认事件；(5) 知识依赖笔记被标记 deprecated/disputed。`lw goal show` 会列出具体原因。

**Q：误解解决为什么报「需要两个不同题面」？**
A：resolved 要求至少两个不同学习对象（不同题面）正确、至少一次无提示、发生在不同日期。用 `lw misconception evidence` 查看当前统计；确需强制解决可用 `--force --reason`（留痕）。

**Q：知识笔记更新了，旧的回答会被改吗？**
A：不会。历史 Attempt 仍引用旧快照，系统只把学习对象标 `needs_review`、目标进 `needs_revalidation`，等你复核后恢复。
